const calcArea = (length, width) => length * width;

const calcPerimeter = (length, width) => 2 * (length + width);

const isSquare = (length, width) => length === width;

export { calcArea, calcPerimeter, isSquare };
