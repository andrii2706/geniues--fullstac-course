import { assert, expect, should } from "chai";
import { describe } from "mocha";

describe("when", () => {
  it("should", () => {
    //очікуємо значення
    expect();
    //
    should;
    //
    assert();
  });
});
